## What this changes

<!-- One or two sentences. -->

## Checklist

- [ ] `make test` passes on macOS
- [ ] `make package-test` passes, if packaging changed
- [ ] CHANGELOG entry added
- [ ] No new privileged arguments, password handling, UI scripting, `sudoers`
      rule, background helper, auto-updater, or runtime dependency

## Privileged changes

<!-- Delete if this PR does not touch bin/authorize.applescript, bin/action, or
     the action map. Otherwise: what changes at the privilege boundary, and why
     is it safe? The maintainer will run docs/MANUAL_TESTING.md before merging. -->

## Testing

<!-- What you ran. If you have no Little Snitch license or no Powerpack, say so
     — `make test` needs neither, and the rest will be run before merging. -->
